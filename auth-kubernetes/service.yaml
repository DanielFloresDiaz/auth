apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: auth
  labels:
    service: auth-service
spec:
  type: ClusterIP
  ports:
    - port: 9999
      targetPort: 9999
      protocol: TCP
  selector:
    app: auth
